<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:components="clr-namespace:RedesSociales.VisualComponents;assembly=RedesSociales"
             x:Class="RedesSociales.Views.LoginView"
             BackgroundColor="{Binding NavigationPrimary}"
             Title="LoginView">
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Button" x:Key="googleButton">
                <Setter Property="BackgroundColor" Value="#EA4335"/>
                <Setter Property="Font" Value="Arial"/>
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Image Source="{components:ImageResource RedesSociales.Images.logo_txt.png}"/>
            <Label Text="{Binding NombrePersona}" />
            <Button 
                Style="{StaticResource googleButton}" 
                ImageSource="{components:ImageResource RedesSociales.Images.logo_google.png}"
                Text="Iniciar Sesión" Command="{Binding InicioSesion}" />
            <Button Text="Inicio Sesion Google" Command="{Binding InicioSesion}"></Button>
            <!--
            <Grid VerticalOptions="Center" HorizontalOptions="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <Image Source="{components:ImageResource RedesSociales.Images.logo_txt.png}" Grid.Row="0"/>
                <Button Style="{StaticResource googleButton}" Grid.Row="1"/>
            </Grid>-->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>