<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             BackgroundColor="{StaticResource GeneralBackgroundColor}"
             NavigationPage.HasNavigationBar="False"
             xmlns:components="clr-namespace:RedesSociales.VisualComponents;assembly=RedesSociales"
             Icon="social_media.png"
             x:Class="RedesSociales.Views.PerfilView">
    
    <ContentPage.Resources>
        
    </ContentPage.Resources>

    <ContentPage.Content>
        <StackLayout Spacing="0">
            
            <StackLayout>

                <Image Source="social_media.png"/>
                
                <StackLayout Orientation="Horizontal">
                    <Label Text="{Binding Usuario.apodo}" />
                    <Label Text="{Binding Usuario.Estado}" />
                </StackLayout>
                
                <FlexLayout JustifyContent="SpaceAround">
                    <StackLayout VerticalOptions="Center">
                        <Label Text="Seguidores" />
                        <Label Text="{Binding Usuario.Seguidores.Count}" HorizontalOptions="Center" />
                    </StackLayout>

                    <StackLayout VerticalOptions="Center">
                        <Label Text="Seguidos" />
                        <Label Text="{Binding Usuario.Seguidos.Count}" HorizontalOptions="Center" />
                    </StackLayout>
                </FlexLayout>

                <Button Text="Editar perfil" Clicked="Buttton_EditarPerfil" />

            </StackLayout>

            <!-- publicaciones del usuario -->
            <ScrollView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                <BoxView BackgroundColor="Yellow" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"/>
            </ScrollView>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>